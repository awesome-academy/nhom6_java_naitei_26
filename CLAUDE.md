@AGENTS.md

## Backend

Claude Code should operate inside the `backend/` directory for development tasks. SunLint Java code quality standards are loaded from `backend/.claude/skills/sunlint-code-quality/`.

## Frontend

Frontend application is located in `frontend/` directory.
